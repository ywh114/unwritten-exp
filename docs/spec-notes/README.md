# Spec notes

Dated amendments to the design spec, written back as implementation teaches
(lab spec §1, rule 5 — "these libraries are the design spec's first contact
with reality").

Naming: `YYYY-MM-DD-<slug>.md`. Each note states what the implementation
taught, which spec section it amends, and the verdict.
