"""K5 experiment — kingdom fixture, demo, and tests for the promise ledger.

The library itself was promoted to `kernel.promise_ledger` (verdict: works);
this directory is its living documentation.
"""

# For convenience, re-export the kernel so `exp.k5_promise_ledger.fixtures`
# still resolves during demos.
