"""Unwritten lab experiments.

Each subdirectory is one standalone experiment (see the lab spec,
unwritten-lab-experiments-repo-spec.md). Experiments are self-contained:
own fixtures, own tests, and a `uv run python -m exp.<name> demo --seed 1`
CLI. Promoted libraries move to kernel/, llm/, or capability/.
"""
