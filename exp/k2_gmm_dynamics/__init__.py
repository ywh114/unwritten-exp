"""K2 experiment — demo, fixtures, and tests for the gmm_dynamics kernel.

The library itself was promoted to `kernel.gmm_dynamics` (verdict: works);
this directory is its living documentation: the toy-village fixture, the
terrain open-question experiment, the demo CLI, and the test suite.
"""
